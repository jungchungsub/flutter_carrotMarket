import 'package:flutter/material.dart';

class NeigborhoodLifePage extends StatelessWidget {
  const NeigborhoodLifePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("NeigborhoodLifePage"),
    );
  }
}
